/** Xu chào mừng khi đăng ký (đồng bộ với src/lib/creditsPolicy.js) */
export const WELCOME_COINS = 100;

/** Hiển thị cảnh báo nạp thêm khi số dư ≤ ngưỡng này */
export const LOW_CREDIT_WARN_THRESHOLD = 20;
